<Project Sdk="Microsoft.NET.Sdk.WindowsDesktop">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net461</TargetFramework>
    <UseWPF>true</UseWPF>
    <AssemblyName>ESDInstaller.Windows8</AssemblyName>
    <RootNamespace>ESDInstaller.Windows8</RootNamespace>
    <Product>ESD Installer</Product>
    <AssemblyTitle>ESD Installer for Windows 8 and 8.1</AssemblyTitle>
    <Description>Install Windows directly to a selected disk partition on Windows 8 and 8.1.</Description>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <PlatformTarget>AnyCPU</PlatformTarget>
    <Prefer32Bit>false</Prefer32Bit>
    <ApplicationIcon Condition="Exists('Assets\ESDInstaller.Windows8.ico')">Assets\ESDInstaller.Windows8.ico</ApplicationIcon>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NETFramework.ReferenceAssemblies.net461" Version="1.0.3" PrivateAssets="all" />
    <ProjectReference Include="..\ESDInstaller.Windows8.Core\ESDInstaller.Windows8.Core.csproj" />
    <ProjectReference Include="..\ESDInstaller.Windows8.Worker\ESDInstaller.Windows8.Worker.csproj" ReferenceOutputAssembly="false" />
    <Reference Include="System.Drawing" />
    <Reference Include="System.Net.Http" />
  </ItemGroup>
  <ItemGroup>
    <Content Include="Strings\**\*.resw"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></Content>
    <Resource Include="Assets\ESDInstaller.Windows8.ico" Condition="Exists('Assets\ESDInstaller.Windows8.ico')" />
  </ItemGroup>
  <Target Name="CopyWorker" AfterTargets="Build">
    <ItemGroup><WorkerFiles Include="..\ESDInstaller.Windows8.Worker\bin\$(Configuration)\net461\**\*" /></ItemGroup>
    <Copy SourceFiles="@(WorkerFiles)" DestinationFiles="@(WorkerFiles->'$(OutDir)Worker\%(RecursiveDir)%(Filename)%(Extension)')" SkipUnchangedFiles="true" />
  </Target>
</Project>
